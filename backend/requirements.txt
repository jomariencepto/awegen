# -----------------------------
# Core Web Framework
# -----------------------------
Flask==2.3.3                  # Web framework: handles routes, requests, and API endpoints
Werkzeug==2.3.7               # WSGI utility library used by Flask internally
gunicorn==21.2.0              # Production WSGI server (Linux/macOS only — not used on Windows)

# -----------------------------
# Database & ORM
# -----------------------------
Flask-SQLAlchemy==3.0.5       # ORM to map Python classes to database tables
Flask-Migrate==4.0.5          # Database migrations for schema changes
PyMySQL==1.1.0                # MySQL driver for connecting Flask to MySQL

# -----------------------------
# Authentication & Security
# -----------------------------
Flask-JWT-Extended==4.5.3     # JWT-based authentication for secure API access
Flask-Cors==4.0.0             # Handle Cross-Origin requests (React frontend ↔ Flask backend)
bcrypt==4.2.1                 # Password hashing (used in utils/hashing.py)

# -----------------------------
# Serialization & Validation
# -----------------------------
marshmallow==3.20.1           # Serialize Python objects to JSON and validate inputs
marshmallow-sqlalchemy==0.29.0# Integrates marshmallow with SQLAlchemy models

# -----------------------------
# NLP & Machine Learning
# -----------------------------
nltk==3.8.1                   # Natural Language Toolkit for basic text processing
spacy==3.8.11                 # Advanced NLP library for tokenization, entity recognition
scikit-learn==1.3.0           # Machine learning library; includes TF-IDF (TfidfVectorizer)
transformers==4.57.3          # Hugging Face Transformers for NLP models (T5, etc.)
torch==2.2.2                  # PyTorch CPU-only; >=2.2.0 required by transformers 4.57.x (register_pytree_node API)
sentencepiece==0.2.1          # Subword tokenization library used by Transformers
sentence-transformers==5.2.2  # Semantic similarity and sentence embeddings
numpy==1.24.3                 # Numerical computing for embeddings and ML operations
huggingface_hub==0.36.2       # Hugging Face Hub for model management and downloads
scipy==1.15.3                 # Scientific computing (dependency of scikit-learn/sentence-transformers)
sympy==1.13.1                 # Symbolic math solver for equation evaluation and solving
Flask-Limiter==3.5.0          # Rate limiting to prevent brute force attacks on auth endpoints

# -----------------------------
# File Handling & Reporting
# -----------------------------
reportlab==4.0.4              # Generate PDF reports and documents
python-docx==0.8.11           # Read/write Microsoft Word documents
msoffcrypto-tool==5.4.2       # Password-protect OOXML files like DOCX without Word
PyPDF2==3.0.1                 # Read and extract text from existing PDF files (fallback)
pdfplumber>=0.9.0             # Advanced PDF extraction: multi-column, tables, better layout (primary)
python-pptx==0.6.21           # Read/write PowerPoint presentations
openpyxl==3.1.2               # Read/write Excel files (.xlsx)
lxml==6.0.2                   # XML/HTML parser (required by python-docx and python-pptx)
Pillow==12.0.0                # Image processing (required by reportlab and python-pptx)
pytesseract==0.3.13           # OCR wrapper for Tesseract engine (requires Tesseract installed on system)
pymupdf>=1.20.0               # PDF image extraction via fitz (used by file_parser.py for embedded images)
docx2pdf==0.1.8               # Convert Word (.docx) to PDF (requires Microsoft Word installed)
pywin32==311                  # Windows COM interface (required by docx2pdf on Windows)

# -----------------------------
# Background Tasks
# -----------------------------
celery==5.3.6                 # Asynchronous task queue for background jobs
redis==4.6.0                  # Message broker used by Celery

# -----------------------------
# Environment Management
# -----------------------------
python-dotenv==1.0.0          # Load environment variables from a .env file

# -----------------------------
# Testing
# -----------------------------
pytest==8.3.3                 # Test framework
pytest-mock==3.14.0           # Convenient mock fixtures for pytest
